This project is an HTML awards reveal deck.

The current starting point is a single bundled HTML export from Claude Design.
The goal is to refactor it into editable source files while preserving the visual output, then add a build/export step that creates one standalone HTML file for distribution.
